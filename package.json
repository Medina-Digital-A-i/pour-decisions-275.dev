{
  "name": "pour-decisions-site",
  "private": true,
  "description": "Pour Decisions juice bar — static PWA on Netlify. menu.json is the source of truth. Functions in netlify/functions use Netlify Blobs for member accounts, spins, and analytics.",
  "scripts": {
    "check": "node scripts/check-menu.js",
    "build": "node scripts/check-menu.js && node scripts/build-site.js",
    "dev": "npx netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "nodemailer": "^6.9.16"
  }
}
